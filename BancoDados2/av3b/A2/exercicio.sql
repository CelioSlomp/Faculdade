CREATE VIEW visao_actor AS SELECT * FROM actor a;
select * from visao_actor;
-- Cria uma visao que recebe todos os itens de actor, logo em seguida
-- é selecionador tudo dessa visao, logo seleciona tudo de actor