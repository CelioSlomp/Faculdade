#include <time.h>
#include <stdio.h>
#include "structs.h"


void adicionarItem(filaPrioridade *no, int val, int prio){
	filaPrioridade *tmp = no->prox;
	// Terminar
}

int main(){

	srand(time(NULL)); // Para não gerar um padrão de resposta

	int r = rand(); 



	printf("primeiro print");
	return 0;
}
